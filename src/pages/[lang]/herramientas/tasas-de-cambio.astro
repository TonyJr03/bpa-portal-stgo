---
/**
 * @pagina   src/pages/[lang]/herramientas/tasas-de-cambio.astro
 * @ruta     /[lang]/herramientas/tasas-de-cambio
 *
 * @descripcion
 *   Página del módulo Tasas de Cambio.
 *   Wrapper fino que delega la lógica reactiva a TasasDeCambio.vue
 *   a través de Herramienta.astro.
 *
 * Estructura:
 *   BLOQUE 1 · HeroText    → tagline + title + subtitle
 *   BLOQUE 2 · Herramienta → isla Vue TasasDeCambio (client:load)
 *   BLOQUE 3 · CallToAction → siguiente paso natural tras consultar tasas
 *   BLOQUE 4 · PageNav     → prev: "Volver a Herramientas"
 *
 * @colecciones  ninguna en build time
 *   monedas → consultada en el cliente por TasasDeCambio.vue
 */

import type { GetStaticPaths } from 'astro';

import Layout from '~/layouts/PageLayout.astro';
import HeroText from '~/components/widgets/HeroText.astro';
import Herramienta from '~/components/sections/Herramienta.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';
import PageNav from '~/components/widgets/PageNav.astro';

import { LANGS, useTranslations } from '~/i18n/utils';
import { getPermalink } from '~/utils/permalinks';

export const getStaticPaths = (() => LANGS.map((lang) => ({ params: { lang } }))) satisfies GetStaticPaths;

const { lang } = Astro.params;
const t = useTranslations(lang);
const p = (slug: string) => getPermalink(slug, lang);

const metadata = {
  title: `${t('tools.rates.hero.title')} — BPA Santiago de Cuba`,
  description: t('tools.rates.hero.subtitle'),
};
---

<Layout metadata={metadata}>
  <!-- BLOQUE 1 · HeroText ──────────────────────────────────────────────────────
      Tagline "Herramientas" sitúa al usuario dentro del módulo.
  ─────────────────────────────────────────────────────────────────────────── -->
  <HeroText
    tagline={t('tools.rates.hero.tagline')}
    title={t('tools.rates.hero.title')}
    subtitle={t('tools.rates.hero.subtitle')}
  />

  <!-- BLOQUE 2 · Herramienta ───────────────────────────────────────────────────
      Monta la isla Vue TasasDeCambio con client:load.
      El wrapper infiere max-w-4xl e inyecta el prop `lang` internamente.
  ─────────────────────────────────────────────────────────────────────────── -->
  <Herramienta herramienta="tasas" id="tasas" />

  <!-- BLOQUE 3 · CallToAction ──────────────────────────────────────────────────
      Dos salidas naturales tras consultar tasas: ir al mapa o contactar.
  ─────────────────────────────────────────────────────────────────────────── -->
  <CallToAction
    title={t('tools.rates.cta.title')}
    subtitle={t('tools.rates.cta.subtitle')}
    actions={[
      {
        variant: 'primary',
        text: t('tools.rates.cta.map'),
        href: p('herramientas/mapa'),
        icon: 'tabler:map-pin',
      },
      {
        variant: 'secondary',
        text: t('tools.rates.cta.contact'),
        href: p('contacto'),
        icon: 'tabler:headset',
      },
    ]}
  />

  <!-- BLOQUE 4 · PageNav ───────────────────────────────────────────────────────
      Solo botón izquierdo: regresa al índice del módulo Herramientas.
  ─────────────────────────────────────────────────────────────────────────── -->
  <PageNav prev={{ text: t('tools.nav.back.tools'), href: p('herramientas') }} />
</Layout>
